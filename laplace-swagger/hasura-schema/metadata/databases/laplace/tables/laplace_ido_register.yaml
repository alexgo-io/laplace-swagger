table:
  schema: laplace
  name: ido_register
select_permissions:
  - role: public
    permission:
      columns:
        - amount
        - apower
        - registered_end
        - registered_start
        - contract_call_contract_id
        - payment_token
        - sender_address
        - tx_id
        - block_height
        - ido_id
      filter: {}
      allow_aggregations: true
