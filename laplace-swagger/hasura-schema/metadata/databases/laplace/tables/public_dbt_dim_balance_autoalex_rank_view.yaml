table:
  schema: public_dbt
  name: dim_balance_autoalex_rank_view
select_permissions:
  - role: public
    permission:
      columns:
        - address
        - name
        - total_autoalex_balance
        - rank_no
        - current_multiplier
      filter: {}
