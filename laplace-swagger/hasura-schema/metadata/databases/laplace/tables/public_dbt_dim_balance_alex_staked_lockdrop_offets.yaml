table:
  schema: public_dbt
  name: dim_balance_alex_staked_lockdrop_offets
select_permissions:
  - role: public
    permission:
      columns:
        - staked_alex_amount
        - block_height
        - address
      filter: {}
