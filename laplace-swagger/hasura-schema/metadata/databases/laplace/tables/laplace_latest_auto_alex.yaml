table:
  schema: laplace
  name: latest_auto_alex
select_permissions:
  - role: public
    permission:
      columns:
        - token_name
        - block_height
        - burn_block_time
        - current_cycle
        - pool_user_id
        - pool_total_staked_stats
        - pool_to_return_stats
        - pool_future_volume_stats
        - apys
        - sync_at
      filter: {}
