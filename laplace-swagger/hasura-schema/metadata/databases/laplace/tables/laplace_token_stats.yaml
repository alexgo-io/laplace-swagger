table:
  schema: laplace
  name: token_stats
select_permissions:
  - role: public
    permission:
      columns:
        - token_name
        - block_height
        - decimals
        - total_supply
        - block_height
      filter: {}
